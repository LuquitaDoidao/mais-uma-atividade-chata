/* 1 */
UPDATE PACIENTE P
SET PESO = 8.5
WHERE P. ID_PACIENTE = 1;

/* 2 */
UPDATE MEDICAMENTOS M
SET ESTOQUE = 50
WHERE M. ID_MEDICAMENTO = 3;

/* 3 */
UPDATE TUTOR T
SET DATA_NASCIMENTO = '2001-05-15'
WHERE T. ID_TUTOR = 2;

/* 4 */
UPDATE AGENDAMENTO A
SET MOTIVO = 'DOR DOOUVIDO'
WHERE A. ID_AGENDAMENTO = 5;

/* 5 */
UPDATE MEDICAMENTOS M
SET DESCRICAO = 'ANTIFLAMATORIO PARA ARTICULACAO'
WHERE M. ID_MEDICAMENTO = 6;

/* 6 */
UPDATE TUTOR T
SET NOME = 'Maria Oliveira'
WHERE T. ID_TUTOR = 3;

/* 7 */
UPDATE MEDICAMENTOS M
SET DESCRICAO = 'APLICAR NO OLHO A CADA 4 HORAS'
WHERE M. ID_MEDICAMENTO = 2;

/* 8 */
UPDATE TUTOR T
SET ENDERECO = 'RUA DOS GIRASSOIS, 123'
WHERE T. ID_TUTOR = 4;

/* 9 */
DELETE FROM AGENDAMENTO A WHERE A. ID_AGENDAMENTO = 7;

/* 10 */
DELETE FROM MEDICAMENTOS M WHERE M. ID_MEDICAMENTO = 8;

/* 11 */
DELETE FROM RECEITA R WHERE R. ID_AGENDAMENTO IN (SELECT A. ID_AGENDAMENTO FROM AGENDAMENTO A WHERE A. ID_PACIENTE in (SELECT P. ID_PACIENTE FROM PACIENTE P WHERE P.ID_TUTOR in (SELECT T. ID_TUTOR FROM TUTOR T WHERE T. ID_TUTOR = 5)));
DELETE FROM AGENDAMENTO A WHERE A. ID_PACIENTE IN (SELECT P. ID_PACIENTE FROM PACIENTE P WHERE P.ID_TUTOR in (SELECT T. ID_TUTOR FROM TUTOR T WHERE T. ID_TUTOR = 5))
DELETE FROM PACIENTE P WHERE P. ID_PACIENTE IN (SELECT T. ID_TUTOR FROM TUTOR T WHERE T. ID_TUTOR = 5)

/* 12 */
DELETE FROM ITENSRECEITA I WHERE I. ID_RECEITA IN (SELECT R. ID_RECEITA FROM RECEITA R WHERE R. ID_AGENDAMENTO IS NULL)
DELETE FROM RECEITA R WHERE R. ID_AGENDAMENTO IS NULL

/* 13 */
DELETE FROM TUTOR T WHERE T. DATA_NASCIMENTO < '1907-01-01'

/* 14 */
DELETE FROM RECEITA R WHERE R. ID_RECEITA >= 7 AND R. ID_RECEITA <= 9;